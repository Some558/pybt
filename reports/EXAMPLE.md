# Example report (truncated)

See a full run output after:

```bash
python run.py orb --market daily --symbol "^N225" --years 10 --cost zero
```

Typical sections in generated reports:

- Baseline metrics (PF, DD, win rate, trades)
- Cost sensitivity table (0×–3×)
- Direction tests (Long / Short / inverted)
- RR parameter neighborhood
- OOS period splits
- Heuristic durable-edge verdict
